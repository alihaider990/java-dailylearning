package day2;
import java.util.Scanner;


public class Main {
    public static void main(String[] args){

String name = "ali haider";
name.length();
name.toLowerCase();
name.toUpperCase();
name.trim();
name.charAt(0);
name.contains("hai");

IO.println(name.length());
        IO.println(name.charAt(0));
        IO.println(name.toLowerCase());
        IO.println(name.toUpperCase());
        IO.println(name.trim());
        IO.println(name.contains("hai"));




    }
}
